import { defineConfig, devices } from '@playwright/test'
export default defineConfig({ testDir: './tests/e2e-adaptive-product-real', timeout: 60_000, expect: { timeout: 20_000 }, retries: 0, fullyParallel: false, reporter: [['list']], use: { baseURL: 'http://127.0.0.1:3104', trace: 'retain-on-failure', screenshot: 'only-on-failure' }, webServer: { command: 'npm run dev -- --hostname 127.0.0.1 --port 3104', url: 'http://127.0.0.1:3104/materias?adaptive-product-real=1', reuseExistingServer: true, timeout: 120_000 }, projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }] })
